# server · 一期服务

需求：`docs/plans/PRD-wenzhen-bazi-phase-one.md` §4.1（账号）、§4.5（研判记录）、§7。
决策：`docs/plans/DECISIONS-wenzhen-bazi-phase-one.md`（D-2 邮箱验证码、D-4 软删除 7 天、D-10 导入试用版 JSON、J-9 导出格式）。

- 账号：邮箱验证码，不设密码，不做短信与微信登录（D-2，2026-09-11）。
- 命例与研判记录归属账号；研判记录软删除，7 天内可恢复。
- 导入：试用版 `cases` 导出的 JSON，按 `id` 去重（J-9）。
- 试用版服务在 `../cloud/`（`node:sqlite`，无账号），已冻结；本目录独立部署，不复用它的表。
- 排盘不在服务端算，与试用版一致：浏览器里跑 `../engine/`，服务端只存输入与口径版本。

技术选型（框架、邮件服务、数据库）在第一次开发任务书里定，未定前不要建脚手架。
